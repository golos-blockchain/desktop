const fs = require('fs')
const rl = require('readline-sync')

const execEx = require('../execEx')

async function buildBlogs() {
    console.log('-- BUILDING ui-blogs')

    const repo = 'ui-blogs'

    console.log('--- Blogs is built.')
}

module.exports = buildBlogs
